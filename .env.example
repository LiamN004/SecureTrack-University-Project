# Local optional settings. Never commit a real .env file.
SECRET_KEY=replace-with-a-long-random-value
FLASK_DEBUG=1
# Leave DATABASE_URL unset locally to use SQLite.
# DATABASE_URL=postgresql://user:password@host:5432/securetrack
